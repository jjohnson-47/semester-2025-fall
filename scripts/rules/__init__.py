"""Rules engine package for course normalization.

Provides typed interfaces and rule implementations that transform raw
course data into a normalized, provenance-rich representation.
"""

from .models import NormalizedCourse, NormalizedField  # noqa: F401

